"""Application deployment version metadata."""

from __future__ import annotations

from myconfig.version import VERSION as APP_VERSION

APP_NAME = "IPQC"
APP_DISPLAY_NAME = f"{APP_NAME} {APP_VERSION}"

