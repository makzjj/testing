# Canonical Pipeline Registry

This registry records what is canonical today, what is still duplicated, and what conditions must be met before older paths are removed.

| Responsibility | Canonical owner/pipeline today | Known duplicate or legacy path | Target end state | Migration trigger | Removal condition |
|---|---|---|---|---|---|
| parameter read/write/read-back verification | `gui/workspace/pages/production_parameter_controller.py` shared `ParameterDefinition` pipeline | Mechanical still keeps local utility orchestration on top of shared definitions; Production test-profile UUID verification remains a separate specialized workflow | one unified parameter pipeline for all parameter types | any parameter-related change touching UUID, PWM, PID, Ramp Down, or future parameters | all live callers use the unified parameter pipeline, regression tests pass, EEPROM behavior is validated live, and no dedicated UUID/PWM wrapper path remains |
| UUID handling | live Production workbook actions use the shared parameter pipeline | runtime node-info UUID inventory and Production test-profile UUID verification remain separate non-programming behaviors | UUID represented only as a parameter definition inside the unified parameter pipeline for parameter programming | next UUID behavior change | all live UUID parameter-programming callers use the unified pipeline, tests pass, live EEPROM behavior is validated, and specialized non-programming UUID paths remain explicitly owned and documented |
| PWM handling | live Production workbook actions use the shared parameter pipeline for PWM definitions | Mechanical keeps a separate PWM utility workflow with shared definitions but page-local orchestration | PWM represented only as a parameter definition inside the unified parameter pipeline | next PWM read/write/verify change | all live PWM parameter-programming callers use the unified pipeline, tests pass, and any remaining PWM path is explicitly owned outside Production workbook programming |
| PID/Ramp handling | `ProductionParameterController` parameter definitions reused by Mechanical page | Mechanical page keeps local orchestration on top of adapter payload handling | PID/Ramp continue through one shared parameter pipeline, with only page-level initiation outside it | next PID or Ramp behavior change | no alternate PID/Ramp read/write/verify pipeline remains and all callers reuse the shared parameter definitions/helpers |
| EEPROM eligibility/save behavior | shared `ProductionParameterController` EEPROM save logic | Mechanical page keeps page-local gating around persistent writes | one shared EEPROM save/settle pipeline with caller-specific entry points only | next EEPROM-related change | all EEPROM save callers use the shared save/settle logic and page-local duplicate save flows are gone |
| frame parsing | `serial_conn/packet_parser.py` | legacy parser/handler code still exists elsewhere, including old dead path in `main_window.py` | one parsing owner for raw UART/CAN-over-UART frames | next parser or packet-shape change | all runtime callers consume parsed packets from the shared parser and dead parser branches are removed |
| semantic command decoding | `data/binary_cmd_parser.py` | local decode logic still exists in pages/controllers/tests for some packet classes | one semantic decoder layer with packet-specific helpers only there | next command decode change | duplicated page/controller decode logic removed or reduced to rendering-only transforms |
| runtime node state | `services/runtime_packet_handler.py` plus `services/node_status_store.py` updating `MainWindow.node_status` | direct page/controller packet subscriptions still bypass runtime state for some behaviors | shared runtime state is the only source for per-node/system status | next runtime state feature or migration step | pages render canonical runtime state and no competing page-local runtime caches remain |
| node discovery | burst CAN scan in `MainWindow.dispatch_node_scan_batch(...)` is the sole scan workflow; `services/node_discovery_coordinator.py` owns cycle state, per-node deduplication, already-scheduled-this-cycle tracking, and reset policy; `MainWindow._schedule_node_info_requests_for_node(...)` is only the legacy integration adapter during the runtime phase | burst lifecycle timing and event-loop integration still live in `main_window.py` | one burst node discovery/info owner during legacy phase, then migrated replacement owner | completed in Phase 0C for scheduling ownership, Phase 0D for sequential-scan removal, and lifecycle closeout for late-response policy; next node discovery change must reuse the burst path and coordinator | sequential scan paths are gone, late packets cannot mutate a completed scan result or trigger new scan-owned node-info work, `cancel_scanning` is removed, tests pass, and remaining `main_window.py` burst lifecycle code is removed only after lifecycle ownership migrates without UI regression |
| firmware/version state | `RuntimePacketHandler` updates per-node firmware in `node_status`; `MainWindow.runtime_system_state["mcu_version"]` is the canonical MCU firmware slot surfaced through `MainWindow.mcu_version` | `CommMonitorDialog` previously kept its own `node_versions` cache; legacy `main_window.py` still renders labels while migration continues | one runtime-owned firmware/version state surfaced to UI | completed in Phase 0B for runtime ownership; next firmware/version change must extend this owner only | old state caches are gone, all callers read runtime-owned firmware/version state, regression tests pass, and the remaining legacy `main_window.py` rendering path is removed only after parity migration |
| emergency state | `RuntimePacketHandler` sets global emergency state on runtime window; workspace shell reads through bridge | legacy/runtime UI coexist; Production shared node-status panel renders badge separately from node LEDs | one shared global emergency state, rendered by UI without altering unrelated node visuals | next emergency behavior change | all emergency UI reads one canonical runtime state and legacy duplicate status handling is removed |
| Single Axis workflow | runtime/global packets remain runtime-owned; `services/functional_transport_adapter.py` is the canonical Single Axis ingress owner; `gui/workspace/controllers/single_axis_functional_test_controller.py` owns workflow state; `gui/workspace/pages/single_axis_functional_popup.py` renders interrupt LEDs from runtime state through `WorkspaceRuntimeBridge` and may opt into `services/release_watch_helper.py` for display freshness only | broad same-node forwarding through `FunctionalTransportAdapter` was tightened in Phase 1D; remaining limitation is protocol ambiguity when the same node can emit identical same-command replies without a request ID | Single Axis controller owns workflow state only and receives only matching operation traffic; popup renders shared runtime interrupt state without owning sensor truth, and any release-watch remains operator-visibility-only rather than workflow correctness | completed in Phase 1D for Single Axis-specific ingress filtering, Phase LED-0D for popup runtime-backed interrupt LEDs, and Phase LED-0E for popup-only release-watch visibility; next motion-routing change must extend this adapter or replace it with a tested general router | popup and live callers use `FunctionalTransportAdapter`, popup LEDs render from canonical runtime state, release-watch does not alter controller correctness, regression tests pass, live validation passes, and the adapter is replaced only when a general router removes the same-node same-command ambiguity without reopening raw global packet exposure |
| Sampling workflow | global/status packets remain runtime-owned; `services/sampling_transport_adapter.py` is the canonical Sampling ingress filter; `gui/workspace/controllers/sampling_test_controller.py` owns workflow state | prior direct Production-page raw `packet_received` subscription was removed; remaining limitation is that this is a narrow Sampling adapter, not yet a general request router | Sampling controller owns workflow state only and receives only matching operation traffic | completed in Phase 1B for Sampling-specific ingress filtering; next Sampling routing change must extend this adapter or replace it with a tested general router | Production page and all Sampling callers use the ingress adapter, regression tests pass, live validation passes, and the narrow adapter is removed only when a general request router replaces it without reopening raw global packet exposure |
| shared motion measurement primitives | `services/motion_measurements.py` owns identical endpoint calculations only: outward endpoint range, return endpoint range, return error, midpoint target, and `Z` / `PZ` safe park target | Single Axis previously encoded an implicit home-zero assumption locally; popup middle-travel preview could overwrite the displayed measured `Range` field | shared endpoint semantics where formulas are identical; workflow sequencing and result lifecycles stay controller-owned | completed in Phase 3B for helper extraction, explicit Single Axis home capture, and popup range-field semantic correction | Single Axis stores a verified explicit home/reference position, Sampling timing/workbook/statistics behavior remains unchanged, the Single Axis `Range` field remains controller-derived measurement data, and any later Sampling aggregate-statistics audit is handled separately |
| Mechanical utility read/write | `gui/workspace/pages/mechanical_page.py` currently owns utility orchestration | local packet listener and local adapter payload handling remain; canonical interrupt LED state is no longer page-owned | Mechanical page starts operations and renders canonical shared state, with utility operations reusing shared pipelines | next Mechanical ownership cleanup | local protocol/runtime caches and duplicate operation handling are removed after tests and live validation |
| console/session state | workspace shell and Production page session panels render current state via bridge/runtime window | `main_window.py` legacy UI still owns overlapping runtime/session displays | one canonical runtime/session state feeding active UI surfaces during migration | next shared session-status change | duplicate rendered session state no longer requires parallel owners |
| physical interrupt state | `services/runtime_packet_handler.py` updates per-node `interrupt_state` inside shared runtime `node_status`; `WorkspaceRuntimeBridge` exposes it to pages | raw packet subscribers still coexist while `main_window.py` remains active; bounded release-watch remains workflow-scoped rather than shared runtime behavior | shared runtime per-node interrupt state with safe operation routing and UI rendering only from canonical state | completed in Phase LED-0A for runtime ownership of `0x81 Z/L`, `0x81 Z/R`, and `0xD8` interrupt responses, then extended in Phase LED-0D so Single Axis popup LEDs also render from runtime state | Mechanical and Single Axis pages render interrupt LEDs from canonical runtime state, no competing page-local interrupt owner remains, tests pass, and any future release-watch polling remains workflow-owned rather than global |
| interrupt release-watch polling | `services/release_watch_helper.py` owns bounded opt-in `0xD8` polling only; runtime interrupt state remains owned by `services/runtime_packet_handler.py` and surfaced through `WorkspaceRuntimeBridge` | Mechanical popup movement actions and Single Axis popup movement visibility now use it; other workflows could still add ad hoc polling if they bypass the helper | one reusable bounded helper for workflow-scoped release confirmation or operator-visible release freshness, with no shared state ownership and no global polling | completed in Phase LED-0B for helper availability, Phase LED-0C for first Mechanical-only adoption, and Phase LED-0E for Single Axis popup operator visibility; next workflow that needs release confirmation must opt into this helper or replace it with an equally narrow tested owner | runtime stays the only interrupt-state owner, Mechanical and Single Axis remain opt-in consumers only, tests pass, and no page/controller introduces competing polling loops |
| NODECONFIG motion polarity consumption | `services/node_motion_polarity.py` is the canonical NODECONFIG motion model; `WorkspaceRuntimeBridge` exposes node-level polarity to pages from runtime node status or project config | Mechanical previously assumed a fixed RUN-sign-to-sensor mapping for release-watch | one canonical NODECONFIG/home-direction interpretation consumed by UI/workflows without local mapping tables | completed in Phase LED-0C-Fix for Mechanical release-watch mapping; next node-motion interpretation change must extend this shared polarity model rather than adding page-local tables | RUN command behavior remains unchanged, no node IDs or rig-specific NODECONFIG examples are hardcoded, tests pass, and pages consume shared polarity data rather than inventing their own sign mapping |
